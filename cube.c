#include <stdio.h>

int main() {

  int n,r,c,cnt=0,arr[1000],t=0;
  scanf("%d",&n);
   for (c = 0; c < n; c++){
    scanf("%d", &arr[c]);

  temp = arr[t];
  if(temp )
   }

   printf("%d",cnt);


  return 0;
}
