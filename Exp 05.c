#include <stdio.h>
int main(){
    int n,i;
  float f;
scanf("%d",&n);
    f=sqrt(n);
    i=f;
   if(i==f)
        printf("%d is a perfect square\n",n);
    else
        printf("%d is not a perfect square\n",n);

    return 0;
}
